datasets:
	mkdir dataset
	cd dataset && wget https://datasets.simula.no/downloads/kvasir-seg.zip && unzip kvasir-seg.zip
	cd dataset && wget https://datasets.simula.no/downloads/kvasir-sessile.zip && unzip kvasir-sessile.zip
